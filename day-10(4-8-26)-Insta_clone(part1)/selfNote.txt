DAY-10(4-8-26) - Instagram Clone (part 1)
Before we Start making a porject, there are some basic thing that we all need to follow:
how every thing going to work, so we first write it
NOTE: some topic that would be good to know like express validator, node maller,imagekit

We are going with monolith folder structure, and for microservices we make little change but the folder name should be same 

(1) What's your porject about : "Instagram Clone"
(2) Dont just start writing code, 1st one is planning
    (2.1) The comman(basic) feature your project should have 
    like: Authentication
          -> rigester
             (1)User ka data save karna
             (2)token should have user data & sign by JWT_SECRET, then give to user
          -> login
          -> logout (New: token backlisting)
          -> OTP based registration (abv)

    Now, What featue your project should have :
    (1) POST api:
       -> create POST
       -> user can see the feed
       -> LIKE POST (new: collection topic)
       -> SAVE POST
    (2) USERs (User related feature) 
       -> followers
       -> following

Now How User schema should look :

user{
    username:Striing,
    bio:String,
    email:{
        type:String,
        unique:true
    }
    password:String,
  //  followers:Array,
  //  following:Array,
    prifilePic:String,
}

My login flow:
1.Final flow (simple)
2.User sends username/email + password.
3.Server finds the user.
4.Server hashes the entered password.
5.Server compares the hash with the stored hash.
6.If they match, create a JWT.
7.Store the JWT in a cookie.
8.Return only safe user information (username, email, bio, profilePic, etc.)—never the password, even if it's hashed.

Note : type is a thing, its happens all the time, and many of the time the error is because of that types, i try to understand the errors and try to solve my self,
get to know many things 

now we are making new folder called Controller as it now going to save all our auth logic : register and login

DAY-11(5-8-26) Part-2
first i write the code again, then instead of useing "crypto" for basic use, low lever package for security, we going to i bcryptjs then req it, then instead of useing crypto.createHash('md5').update(password).digest('hex'),we are going to use const hash = bcrypt.hash(password,10), 10 is salt, layer of hashing
and instead of writing hast for pass in login then check it hash with pass again, we can just use bcrypt for both we can use with a method called .compare(password,user.password)

now for today is insta post : (we will do logout letter)
(1) create post 
{
    caption:String,
    img_url:String, (and how to handle on our express server)
    user:userId,
    createdAt:Date-time
}
NOTE: we will do like and comment in part 3

and to create a api we create in routes like post.routers.js

now postRouter.post('/', postController.createPostController); showing undefine as its not raw, its form data now where we send caption and image url and express dont read it by default and as express also dont read raw too, for that we use middleware -> app.use(express.json()) 
for now we need to i multer: as express dont read file by default, by multer it has power now, as want to send data in form data

for that we use multer : which uses two type of storages 
1. disk storeage -> store in server
2. memory storage -> store in server(ram) -> server temp store the file and when th file get to the cloud stroge then server delete or remove the file and for that we use: const upload = multer.({storage:multer.memoryStroage()})

[Object: null prototype] { caption: 'test1' }
  fieldname: 'image',
  originalname: '704387.png',
  encoding: '7bit',
  mimetype: 'image/png',
  buffer: <Buffer 89 50 4e 47 0d 0a 1a 0a 00 00 00 0d 49 48 44 52 00 00 0a 00 00 00 06 40 08 02 00 00 00 3a ce 8c 97 00 00 00 19 74 45 58 74 53 6f 66 74 77 61 72 65 00 ... 5155170 more bytes>,
  size: 5155220

lets suppose we a user with 20000 followers and he store the img in server which is 100kb, means server need to send this img to 20000 followers as it become 200md means server need 200md bandwidth(like how much data its transfer), as server also have bandwidth pricing, and to avoid the high pricing we use cloud storage provider, like from aws -> s3, and imagekit, cloudinary -> independent