const express = require('express'); // to use the express package we need require and store all express function.. on express variable

const app = express();   // instance of server is created then store in app

app.listen(3000)   // now telling server to start on port 3000